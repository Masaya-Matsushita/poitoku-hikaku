# AGENTS.md

このファイルはAI Agent（Claude Code, Cursor等）がセッション開始時に自動で読み込みます。

---

## プロジェクト概要

**ポイ得比較** - ポイントサイト（ハピタス、モッピー等）の案件を横断検索・比較できるWebサービス

| 項目 | 内容 |
|------|------|
| 現在のフェーズ | PoC（Crawl4AIの精度検証） |
| 最重要方針 | メンテナンスを極力減らす |
| 技術スタック | Next.js + Python (Crawl4AI) + Supabase |

---

## ドキュメント構成

このプロジェクトは **Single Source of Truth (SSOT)** の原則に従っています。

### 主要ドキュメント

| ドキュメント | 内容 | 参照タイミング |
|-------------|------|-----------------|
| `SPEC.md` | **確定した仕様のみ**（技術構成、DB設計、要件） | 実装時に必ず参照 |
| `docs/setup.md` | 環境構築の実行手順 | 環境構築時 |

### 参照資料（低頻度）

| ドキュメント | 内容 |
|-------------|------|
| `docs/reference/competitor.md` | 競合分析 |
| `docs/reference/point-sites.md` | ポイントサイト詳細情報 |
| `docs/reference/cost-estimation.md` | コスト試算 |

### AI開発用

| ディレクトリ | 内容 |
|-------------|------|
| `ai/tasks/` | 依頼済みタスク（request.md + plan.md） |
| `ai/backlog/` | 未依頼のアイデア・改善案 |
| `logs/meetings/` | 会議記録（日付-内容.md） |

---

## AI向けルール

### 1. 仕様変更時

確定した仕様変更は **SPEC.md のみ** を更新する。

| 変更内容 | 更新先 |
|----------|--------|
| 技術的決定（DB設計など） | `SPEC.md` |
| 要件変更 | `SPEC.md` |
| 将来検討事項 | `ai/backlog/` に新規ファイル作成 |

**更新後は必ずユーザーに報告する。**

### 2. コーディング規約

**共通**
- 過度な抽象化を避け、シンプルに
- コメントは日本語OK

**TypeScript（フロントエンド）**
- App Router、shadcn/ui、Tailwind CSS
- 型定義を省略しない

**Python（クローラー）**
- 型ヒント必須
- Pydantic でスキーマ定義
- リクエスト間隔3秒以上

### 3. コミット

- メッセージは日本語OK
- 1コミット = 1論理変更
- ドキュメント更新も含める

### 4. プロジェクト固有の注意

- ポイント換算: モッピー・ハピタスは 1pt = 1円
- クローリング: robots.txt遵守、1日1回、リクエスト間隔3秒以上
- エラー時: 429/403は即時中断、5xx系は連続3回で中断（サーキットブレーカー）
- 障害時: 古いデータを保持し、鮮度を表示

### 5. 禁止事項

以下のファイルは **絶対に読み込まないこと**:

- `.env`
- `.env.local`
- `.env.*`
- `secrets/` 配下
